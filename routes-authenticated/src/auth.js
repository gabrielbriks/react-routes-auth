  export const isAuthenticated =  () => {
    //aqui fica toda  logica de autenticação. 
    //Ex: Ir o localstorage verificar se o usuario possui o token(JWT) e etc.

    /* EXEMPLO PRÁTICO

    export const isAuthenticated = () =>  {
      if (localStorage.getItem('TOKEN')) {
          return true
      } else {
          return false
      }
    }
    */


    return true
  };